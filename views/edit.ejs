<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="bg-zinc-900 min-h-screen w-full text-white p-8">
      <div class="w-full flex justify-between">
        <a class="text-violet-500" href="/allposts">All posts -></a>
        <a class="bg-red-500 rounded-lg hover:bg-red-700 p-3" href="/logout"
          >Logout</a
        >
      </div>
      <p class="mb-4">Edit post</p>
      <form method="post" action="/update/<%= post._id %>">
        <textarea
          placeholder="What's in your mind ?"
          class="block resize-none w-1/3 p-3 rounded-md outline-none bg-transparent border-2 border-zinc-500"
          name="content"
        ><%= post.content%></textarea>
        <input
          type="submit"
          value="Edit Post"
          class="block mt-2 text-black rounded-md cursor-pointer bg-yellow-500 p-2"
        />
      </form>
        
      </div>
    </div>
  </body>
</html>
